/**
 * Drizzle schema — mirrors supabase/migrations/*.sql.
 * Populated in S2 (master plan §4).
 *
 * Authoritative source of truth: SQL migrations.
 * This file provides typed access only.
 */

export {};
